#PBS -l walltime=%s
#PBS -l nodes=%s
#PBS -N %s
hostname
DIR="%s"
TDIR="%s"
cd $HOME/jscfi/"$DIR"/"$TDIR" || exit 1
date >> log
hostname >> log
echo "PBS_O_WORKDIR=" >> log
cp "$PBS_NODEFILE" pbs-nodes
NP=`wc -l pbs-nodes | awk '{print $1}'`
LD_LIBRARY_PATH=/share/mvapich-gcc/lib/shared/ /usr/bin/time -f '%%e' -o timing /share/mvapich-gcc/bin/mpirun_rsh -np $NP -hostfile `pwd`/pbs-nodes /home/shukela/bin/lmp_my < source.c 2> stderr > stdout
date > completed
